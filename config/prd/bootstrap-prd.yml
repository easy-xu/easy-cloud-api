spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-headless:3306/cloud?useUnicode=true&characterEncoding=utf8
    username: ENC(J2VWgLMJqeJ8735mAR7ZzQ==)
    password: ENC(tOQ1zdQZW5n+CA0IeizLPP6tZp4OD6Cd)
  cloud:
    nacos:
      discovery:
        # 命名空间
        namespace: ${spring.profiles.active}
        # 服务注册地址
        server-addr: nacos-standalone-headless.middle-ware:8848
      config:
        # 命名空间
        namespace: ${spring.profiles.active}
        # 配置中心地址
        server-addr: nacos-standalone-headless.middle-ware:8848
        # 配置文件格式
        file-extension: yml
