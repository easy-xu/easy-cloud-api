# Spring
spring:
  application:
    # 应用名称
    name: dev-service
  profiles:
    # 环境配置
    active: dev

  jackson:
    default-property-inclusion: non_null
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

#mybatis
mybatis-plus:
  typeEnumsPackage: pro.simplecloud.*.enums

logging:
  level:
    pro.simplecloud.*.mapper: debug


