# Spring
spring:
  application:
    # 应用名称
    name: service-gateway
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      discovery:
        # 命名空间
        namespace: ${spring.profiles.active}
        # 服务注册地址
        server-addr: 110.40.249.62:30848
      config:
        # 命名空间
        namespace: ${spring.profiles.active}
        # 配置中心地址
        server-addr: 110.40.249.62:30848
        # 配置文件格式
        file-extension: yml


